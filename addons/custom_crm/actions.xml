   <odoo>

    <!-- quitar  actions no es por id, se especifica en el view_mode -->
    <record id="crm.crm_lead_action_pipeline" model="ir.actions.act_window">
        <field name="view_mode">kanban,list</field>
    </record>
   </odoo>